function solve(elements) {
    let newArray = [];
    for(let i = 0; i < ' '; i++) {
        newArray.push(elements[i]); 
        console.log(newArrive.join)
    }
    let result = '';

    for (let i = newArray.length - 1; i >= 0; i --) {
        result += newArray[i] + ' '; 
    }
    console.log(result)
};
solve(['a', 'b', 'c', 'd', 'e']);
solve(['abc', 'def', 'hig', 'klm', 'nop']);
solve(['33', '123', '0', 'dd']);